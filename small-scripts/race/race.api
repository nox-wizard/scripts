
native race_getProperty( const race, const property, const subProp=0, ... );

native race_getGlobalProp( const property, const subProp=0, ... );

public __race_enlist( const chr )
{
	chr_message( chr, _, "Race system work in progress" );
}